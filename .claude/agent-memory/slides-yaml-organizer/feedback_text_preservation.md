---
name: Preserve existing story text
description: Never replace bilingual text on story slides — only update photo paths
type: feedback
---

When adding media to existing ceremony sections, preserve the existing bilingual text content on story slides. Only update the `photo` path to point to the new media.

**Why:** The user corrected the agent when it proposed replacing existing text. The descriptions were carefully written and should be leveraged, not discarded.
**How to apply:** When modifying a story slide, keep all `text` content intact. Only change `photo`, `layout`, or add new slides around it.
